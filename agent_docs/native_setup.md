# Native Setup

No build issues encountered during installation of vision-camera and fast-tflite. To compile the native app on your machine, ensure Android SDK is configured and run:

```bash
npx expo run:android
```

Make sure to rebuild if any native module changes.
